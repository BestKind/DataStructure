###BF算法
#####Brute-Force算法的基本思想是：

1. 从主串的第一个字符起和模式子串的第一个字符进行比较，若相等则继续逐个比较后续字符，否则从主串的第二个字符起再重新和子串进行比较。

2. 依此类推，直至子串中的每个字符依次和主串的一个连续的字符序列相等，则称模式匹配成功，此时子串的第一个字符在主串中的位置就是子串在主串中的位置，否则模式匹配不成功。

#####Brute-Force算法的实现
![](https://github.com/312998164/DataStructure/blob/master/Images/brute_force.jpg)
